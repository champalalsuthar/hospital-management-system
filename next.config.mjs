// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;

// // const withCSS = require('@zeit/next-css');
// // module.exports = withCSS();/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['res.cloudinary.com'], // Add your Cloudinary domain here
    },
};

export default nextConfig;
